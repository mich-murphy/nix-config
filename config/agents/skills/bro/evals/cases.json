[
  {
    "id": "bro-preserves-operational-detail",
    "skill": "bro",
    "prompt": "The previous assistant response was:\n\n## Deployment\nThe worker uses exponential backoff, meaning every retry waits longer than the previous one. Edit `/srv/app/config.toml`, then run:\n\n```sh\ncargo test -p worker\n```\n\n**Warning:** Never run `git reset --hard` here because it would destroy uncommitted work. If the focused test passes, deploy to staging next.\n\nThe user replied: bro\n\nRewrite the previous response for that user.",
    "assertions": [
      {
        "type": "contains",
        "value": "/srv/app/config.toml"
      },
      {
        "type": "contains",
        "value": "cargo test -p worker"
      },
      {
        "type": "contains",
        "value": "git reset --hard"
      },
      {
        "type": "contains_any",
        "values": [
          "destroy",
          "delete",
          "lose",
          "wipe"
        ]
      },
      {
        "type": "contains",
        "value": "staging"
      },
      {
        "type": "not_contains",
        "value": "this skill"
      },
      {
        "type": "max_words",
        "value": 105
      }
    ],
    "split": "development",
    "kind": "outcome"
  },
  {
    "id": "bro-keeps-simple-answer-simple",
    "skill": "bro",
    "prompt": "The previous assistant response was: An idempotent request can be repeated without changing the result after the first successful call. This matters when a client retries after a timeout because the server may already have completed the operation.\n\nThe user replied: Can you say that more simply?\n\nRewrite the previous response for that user.",
    "assertions": [
      {
        "type": "contains_any",
        "values": [
          "repeat",
          "again",
          "retry"
        ]
      },
      {
        "type": "contains_any",
        "values": [
          "same result",
          "won't change",
          "won’t change",
          "does not change",
          "doesn't change",
          "extra changes"
        ]
      },
      {
        "type": "contains_any",
        "values": [
          "timeout",
          "times out"
        ]
      },
      {
        "type": "not_regex",
        "pattern": "^#{1,6}\\s"
      },
      {
        "type": "max_words",
        "value": 55
      }
    ],
    "split": "held-out",
    "kind": "outcome"
  },
  {
    "id": "bro-preserves-security-sequence",
    "skill": "bro",
    "split": "held-out",
    "kind": "outcome",
    "prompt": "The previous assistant response was: Rotate the leaked API key first, then invalidate active sessions. Do not paste the replacement key into chat or commit it to `.env`. After rotation, run `./scripts/smoke-auth.sh` and record only whether it passed.\n\nThe user replied: bro\n\nRewrite the previous response for that user.",
    "assertions": [
      {"type": "contains", "value": "Rotate"},
      {"type": "contains", "value": "invalidate"},
      {"type": "contains", "value": ".env"},
      {"type": "contains", "value": "./scripts/smoke-auth.sh"},
      {"type": "not_contains", "value": "this skill"},
      {"type": "max_words", "value": 75}
    ]
  }
]
