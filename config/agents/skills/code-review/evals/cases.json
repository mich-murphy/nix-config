[
  {
    "id": "route-missing-plan-decision",
    "split": "development",
    "kind": "outcome",
    "prompt": "Review a candidate that rewrites persisted records, but the supplied plan never decides whether old records may change and gives no migration behavior. Return only JSON matching the ship review schema with one blocking finding.",
    "assertions": [
      {"type": "json_equals", "path": "verdict", "value": "replan"},
      {"type": "json_equals", "path": "findings.0.severity", "value": "blocking"},
      {"type": "json_in", "path": "findings.0.route", "values": ["replan", "clarify"]},
      {"type": "contains_any", "values": ["persist", "migration", "record", "plan"]}
    ]
  },
  {
    "id": "route-behavior-defect-to-tdd",
    "split": "development",
    "kind": "outcome",
    "prompt": "The plan says timeout returns RetryableError. The diff returns None on timeout, while tests cover only success. Return only JSON matching the ship review schema.",
    "assertions": [
      {"type": "json_equals", "path": "verdict", "value": "changes-required"},
      {"type": "json_equals", "path": "findings.0.route", "value": "tdd"},
      {"type": "json_equals", "path": "findings.0.severity", "value": "blocking"},
      {"type": "contains_any", "values": ["timeout", "RetryableError", "None"]}
    ]
  },
  {
    "id": "route-maintainability-defect-to-refactor",
    "split": "held-out",
    "kind": "outcome",
    "prompt": "Behavior matches the plan and tests pass, but one validation policy is duplicated in four branches; the next approved variant would require changing all four. Return only JSON matching the ship review schema.",
    "assertions": [
      {"type": "json_equals", "path": "verdict", "value": "changes-required"},
      {"type": "json_equals", "path": "findings.0.route", "value": "refactor"},
      {"type": "contains_any", "values": ["duplicat", "four", "change"]},
      {"type": "contains_any", "values": ["maintain", "local", "policy", "authority"]}
    ]
  },
  {
    "id": "allow-no-finding-review",
    "split": "held-out",
    "kind": "outcome",
    "prompt": "The focused candidate exactly implements one validation case, preserves the public contract, includes a meaningful regression test, passes required checks, and inspection reveals no material issue. Return only JSON matching the ship review schema.",
    "assertions": [
      {"type": "json_equals", "path": "verdict", "value": "pass"},
      {"type": "json_equals", "path": "findings", "value": []},
      {"type": "contains_any", "values": ["pass", "no material", "satisfies"]}
    ]
  }
]
