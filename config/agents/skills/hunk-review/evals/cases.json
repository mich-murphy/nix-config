[
  {
    "id": "hunk-discovers-session-safely",
    "skill": "hunk-review",
    "prompt": "A user says: 'Hunk is open for workspace/project. Walk me through the current diff.' You are in a network sandbox. Do not execute anything. Return only JSON with keys `first_command`, `access`, and `launch_tui`, where `access` is the access/approval strategy and `launch_tui` is a boolean.",
    "assertions": [
      {
        "type": "json_equals",
        "path": "first_command",
        "value": "hunk session list --json"
      },
      {
        "type": "contains_any",
        "values": [
          "outside",
          "approval",
          "escalat"
        ]
      },
      {
        "type": "contains",
        "value": "hunk session"
      },
      {
        "type": "json_equals",
        "path": "launch_tui",
        "value": false
      }
    ],
    "split": "development",
    "kind": "outcome"
  },
  {
    "id": "hunk-inspects-before-patch",
    "skill": "hunk-review",
    "prompt": "Session discovery returned one matching session with ID `s-42` and Repo `workspace/project`. The user asks you to review it, but has not asked for comments or navigation. Do not execute anything. Return only JSON with keys `commands` (an ordered array) and `mutate_comments` (boolean). Include only the minimum commands needed to confirm the session and inspect its file/hunk structure before raw patch text is known to be necessary.",
    "assertions": [
      {
        "type": "json_equals",
        "path": "commands.0",
        "value": "hunk session get s-42 --json"
      },
      {
        "type": "json_equals",
        "path": "commands.1",
        "value": "hunk session review s-42 --json"
      },
      {
        "type": "json_equals",
        "path": "mutate_comments",
        "value": false
      },
      {
        "type": "not_contains",
        "value": "--include-patch"
      },
      {
        "type": "not_contains",
        "value": "hunk diff"
      }
    ],
    "split": "held-out",
    "kind": "outcome"
  },
  {
    "id": "hunk-comment-only-when-requested",
    "skill": "hunk-review",
    "split": "held-out",
    "kind": "outcome",
    "prompt": "The current Hunk session is `s-9`. The user explicitly asks for one focused inline comment on new line 31 of `src/auth.ts`, with summary `Handle missing token` and rationale `The parser can return undefined`. Do not execute anything. Return only JSON with keys `command`, `clears_existing`, and `launch_tui`.",
    "assertions": [
      {"type": "contains", "value": "hunk session comment add s-9"},
      {"type": "contains", "value": "--file src/auth.ts"},
      {"type": "contains", "value": "--new-line 31"},
      {"type": "json_equals", "path": "clears_existing", "value": false},
      {"type": "json_equals", "path": "launch_tui", "value": false}
    ]
  }
]
