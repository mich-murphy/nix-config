interface:
  display_name: "Neo Discover"
  short_description: "Ground a Neo planning task in evidence"
  default_prompt: "Use $neo-discover with the handoff generated by $neo to clarify this task."
policy:
  allow_implicit_invocation: false
