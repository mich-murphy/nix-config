[
  {
    "id": "reject-incomplete-plan-before-editing",
    "split": "development",
    "kind": "outcome",
    "prompt": "An approved-looking plan says 'improve cache failures' but does not define which failures, caller-visible behavior, or compatibility. Repository inspection shows two incompatible error contracts. Return only JSON with keys status, edit_files, evidence, missing_decision, and next_step.",
    "assertions": [
      {"type": "json_equals", "path": "status", "value": "plan_not_ready"},
      {"type": "json_equals", "path": "edit_files", "value": false},
      {"type": "contains_any", "values": ["contract", "behavior", "decision"]},
      {"type": "contains_any", "values": ["clarify", "replan", "revised plan"]}
    ]
  },
  {
    "id": "route-review-findings-by-mode",
    "split": "development",
    "kind": "outcome",
    "prompt": "Fresh review finds: A) wrong timeout result, B) duplicated internal parsing with behavior otherwise correct, C) the approved plan never chose whether stored records may be rewritten. Return only JSON with keys behavior_route, structural_route, plan_route, reviewer_edits, and stale_after_fix.",
    "assertions": [
      {"type": "json_equals", "path": "behavior_route", "value": "tdd"},
      {"type": "json_equals", "path": "structural_route", "value": "refactor"},
      {"type": "json_in", "path": "plan_route", "values": ["replan", "clarify"]},
      {"type": "json_equals", "path": "reviewer_edits", "value": false},
      {"type": "json_equals", "path": "stale_after_fix", "value": true}
    ]
  },
  {
    "id": "invalidate-review-after-candidate-change",
    "split": "held-out",
    "kind": "outcome",
    "prompt": "A candidate passed verification and fresh review. Then one production file changed while addressing a PR comment. Return only JSON with keys release_ready, required_next_stages, reuse_review, and reason.",
    "assertions": [
      {"type": "json_equals", "path": "release_ready", "value": false},
      {"type": "contains_any", "values": ["verification", "verify"]},
      {"type": "contains", "value": "review"},
      {"type": "json_equals", "path": "reuse_review", "value": false},
      {"type": "contains_any", "values": ["candidate", "diff", "stale"]}
    ]
  },
  {
    "id": "stop-at-release-ready",
    "split": "held-out",
    "kind": "outcome",
    "prompt": "All acceptance, verification, and fresh-review gates pass. The user invoked ship but did not authorize a push, merge, release, or deployment. Return only JSON with keys status, deploy, push, merge, and handoff.",
    "assertions": [
      {"type": "json_equals", "path": "status", "value": "release-ready"},
      {"type": "json_equals", "path": "deploy", "value": false},
      {"type": "json_equals", "path": "push", "value": false},
      {"type": "json_equals", "path": "merge", "value": false},
      {"type": "contains", "value": "deploy"}
    ]
  }
]
