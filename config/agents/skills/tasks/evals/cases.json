[
  {
    "id": "verticalize-horizontal-neo-delivery",
    "split": "development",
    "kind": "outcome",
    "origin": {
      "trace_reference": "corpus:design-before-code#delivery-design",
      "redaction_version": "not-applicable-public-corpus",
      "deduplication_hash": "verticalize-horizontal-neo-delivery-v1"
    },
    "prompt": "Use $tasks. An approved Neo brief adds user-configurable cache expiry across a web form, API, service, and database. Its delivery section says: task A database changes, task B service and API, task C UI, task D tests. The public behavior, data semantics, rollback, and verifiers are settled. Return only JSON with keys status, unit_shape, horizontal_batches, warning_tokens, reserve_percent, context_guarantee, source_plan_edited, external_tickets_created, and task_outcomes.",
    "skill_eligible": true,
    "risk_class": "medium",
    "acceptance_manifest": [
      "Replace horizontal batches with independently verifiable vertical behavior tasks.",
      "Use the conservative default context policy without claiming a guarantee.",
      "Preserve the approved source and avoid external side effects."
    ],
    "assertions": [
      {"type": "json_equals", "path": "status", "value": "task_graph_ready"},
      {"type": "json_equals", "path": "unit_shape", "value": "vertical"},
      {"type": "json_equals", "path": "horizontal_batches", "value": false},
      {"type": "json_equals", "path": "warning_tokens", "value": 100000},
      {"type": "json_equals", "path": "reserve_percent", "value": 50},
      {"type": "json_equals", "path": "context_guarantee", "value": false},
      {"type": "json_equals", "path": "source_plan_edited", "value": false},
      {"type": "json_equals", "path": "external_tickets_created", "value": false},
      {"type": "json_truthy", "path": "task_outcomes"}
    ]
  },
  {
    "id": "do-not-fragment-bounded-plan",
    "split": "development",
    "kind": "outcome",
    "origin": {
      "trace_reference": "corpus:dillon-workflow#ticket-graph",
      "redaction_version": "not-applicable-public-corpus",
      "deduplication_hash": "do-not-fragment-bounded-plan-v1"
    },
    "prompt": "Use $tasks. The approved plan has one observable behavior, one public seam, a focused test command, no migration, and one small implementation surface. Repository inspection confirms it is independently Ship-ready and well below the context warning. Return only JSON with keys status, task_count, split, reason, source_plan_edited, and invoked_ship.",
    "skill_eligible": true,
    "risk_class": "low",
    "acceptance_manifest": [
      "Avoid decomposition that adds handoff cost without a real boundary.",
      "Return one task plan and do not implement it."
    ],
    "assertions": [
      {"type": "json_equals", "path": "status", "value": "task_graph_ready"},
      {"type": "json_equals", "path": "task_count", "value": 1},
      {"type": "json_equals", "path": "split", "value": false},
      {"type": "contains_any", "values": ["bounded", "one outcome", "handoff", "well below"]},
      {"type": "json_equals", "path": "source_plan_edited", "value": false},
      {"type": "json_equals", "path": "invoked_ship", "value": false}
    ]
  },
  {
    "id": "stop-on-unsettled-data-contract",
    "split": "held-out",
    "kind": "outcome",
    "origin": {
      "trace_reference": "local:ship/readiness-and-stops",
      "redaction_version": "not-applicable-package-contract",
      "deduplication_hash": "stop-on-unsettled-data-contract-v1"
    },
    "prompt": "Use $tasks. A plan marked approved says to migrate stored timestamps but leaves open whether existing naive values mean UTC or tenant-local time. Either answer changes compatibility and rollback. Return only JSON with keys status, task_count, artifacts_written, missing_decision, impact, requested_handoff, and decision_invented.",
    "skill_eligible": true,
    "risk_class": "high",
    "acceptance_manifest": [
      "Reject decomposition because a persistent-data decision is unresolved.",
      "Name the exact upstream decision and create no task artifacts."
    ],
    "assertions": [
      {"type": "json_in", "path": "status", "values": ["plan_not_ready", "replan_required"]},
      {"type": "json_equals", "path": "task_count", "value": 0},
      {"type": "json_equals", "path": "artifacts_written", "value": false},
      {"type": "contains_any", "values": ["UTC", "tenant-local", "timestamp", "data semantics"]},
      {"type": "contains_any", "values": ["compatibility", "rollback", "stored"]},
      {"type": "json_equals", "path": "decision_invented", "value": false}
    ]
  },
  {
    "id": "phase-wide-mechanical-migration",
    "split": "held-out",
    "kind": "outcome",
    "origin": {
      "trace_reference": "corpus:dillon-workflow#ticket-graph",
      "redaction_version": "not-applicable-public-corpus",
      "deduplication_hash": "phase-wide-mechanical-migration-v1"
    },
    "prompt": "Use $tasks. An approved plan replaces a serialized field used by 60 callers. Mixed versions must remain compatible, and the plan explicitly approves expand-migrate-contract because no narrow user behavior can move every caller safely. Each phase has a verifier and rollback. Return only JSON with keys status, strategy, phases, dependency_ordered, each_phase_coherent, context_guarantee, and external_actions.",
    "skill_eligible": true,
    "risk_class": "high",
    "acceptance_manifest": [
      "Use the approved migration exception rather than forcing a fake vertical slice.",
      "Create coherent dependency-ordered phases and preserve the context claim boundary."
    ],
    "assertions": [
      {"type": "json_equals", "path": "status", "value": "task_graph_ready"},
      {"type": "json_equals", "path": "strategy", "value": "expand-migrate-contract"},
      {"type": "json_equals", "path": "phases.0", "value": "expand"},
      {"type": "json_equals", "path": "phases.1", "value": "migrate"},
      {"type": "json_equals", "path": "phases.2", "value": "contract"},
      {"type": "json_equals", "path": "dependency_ordered", "value": true},
      {"type": "json_equals", "path": "each_phase_coherent", "value": true},
      {"type": "json_equals", "path": "context_guarantee", "value": false},
      {"type": "json_equals", "path": "external_actions", "value": false}
    ]
  }
]
